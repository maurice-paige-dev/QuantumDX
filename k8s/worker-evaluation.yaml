apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantumdx-evaluation-worker
  namespace: quantumdx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantumdx-evaluation-worker
  template:
    metadata:
      labels:
        app: quantumdx-evaluation-worker
    spec:
      containers:
      - name: worker
        image: quantumdx/api:latest
        command: ["python", "-m", "workers.run_worker", "evaluation"]
        envFrom:
        - configMapRef: {name: quantumdx-config}
        volumeMounts:
        - name: data
          mountPath: /data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: quantumdx-data
