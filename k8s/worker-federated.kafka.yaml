apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantumdx-federated-worker
  namespace: quantumdx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantumdx-federated-worker
  template:
    metadata:
      labels:
        app: quantumdx-federated-worker
    spec:
      containers:
        - name: worker
          image: quantumdx/api:latest
          command: ["python", "-m", "workers.run_worker", "federated"]
          envFrom:
            - configMapRef:
                name: quantumdx-config
            - secretRef:
                name: quantumdx-secrets
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: quantumdx-data
