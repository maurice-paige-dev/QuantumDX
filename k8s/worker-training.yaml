apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantumdx-training-worker
  namespace: quantumdx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantumdx-training-worker
  template:
    metadata:
      labels:
        app: quantumdx-training-worker
    spec:
      containers:
      - name: worker
        image: quantumdx/api:latest
        command: ["python", "-m", "workers.run_worker", "training"]
        envFrom:
        - configMapRef: {name: quantumdx-config}
        volumeMounts:
        - name: data
          mountPath: /data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: quantumdx-data
