[metadata]
name = quantumdx
version = 0.0.1

[options]
packages = find:
where=.
include=quantumdx*
exclude=quantumdx.tests
install_requires =
    requests
    importlib-metadata; python_version<"3.10"